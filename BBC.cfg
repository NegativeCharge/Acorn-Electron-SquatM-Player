MEMORY {
    ZP:  file = "", define = yes, start = $0070, size = $002F;
    RAM: file = %O,               start = $1100, size = $6B00;
}
SEGMENTS {
    ZEROPAGE: load = ZP,      type = zp,  optional = yes;
    CODE:     load = RAM,     type = rw,                  define = yes;
}
